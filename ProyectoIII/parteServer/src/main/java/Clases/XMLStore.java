/*
package Clases;

import java.util.HashMap;
import java.util.Map;

public class XMLStore {
    private Map<String, Map<String, Object>> instances;

    public XMLStore() {
        this.instances = new HashMap<>();
    }

    public void insertInstance(String instanceId, Map<String, Object> instanceData) {
        instances.put(instanceId, instanceData);
    }

    public void deleteInstance(String instanceId) {
        instances.remove(instanceId);
    }

    public boolean containsInstance(String instanceId) {
        return instances.containsKey(instanceId);
    }

    // Otros métodos y lógica necesaria para manipular las instancias de XMLStore
}
*/